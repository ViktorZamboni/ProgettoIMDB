COMANDI:
https://nodejs.org/en/download/ - Installare node

npm install -g @angular/cli = installa angular
npm install = installa le dipendenze

ng new nomeProgetto = crea un nuovo progetto angular
ng serve OPPURE npm start = avvia il server 

ng generate component nome-componente --standalone = crea un nuovo componente 